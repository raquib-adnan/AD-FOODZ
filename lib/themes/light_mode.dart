import 'package:flutter/material.dart';

ThemeData lightmode = ThemeData(
  colorScheme: const ColorScheme.light(
    inversePrimary: Color(
        0xFF5D4037), // Warm Brown for text colors (font), resembling baked goods or spices
    surface: Color(
        0xFFFFF9C4), // Light Creamy Yellow for the surface, soft and inviting
    tertiary: Color(
        0xFFFFD54F), // Sunny Yellow for buttons, highlights, creating a bright and cheerful atmosphere
    primary: Color(
        0xFFFFC107), // Golden Yellow for primary elements, representing energy, and richness
    secondary: Color(
        0xFFFFA000), // Deeper Amber for secondary elements, adding depth and contrast
  ),
);
